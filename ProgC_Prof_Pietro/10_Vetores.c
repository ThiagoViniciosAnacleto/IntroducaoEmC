#include <stdio.h>

int main(){

    int vetor[5] = { 10, 20, 30, 40, 50}; // aqui eu seto valores de cada variavel do vetor //vetor = {10, 20, 30, 40, 50} está ERRADO!!


    printf (" %d, %d, %d, %d, %d", vetor[0], vetor[1], vetor[2], vetor[3], vetor[4] ); //aqui estou exibindo o valor de cada variavel já setada no vetor


    return 0;
}